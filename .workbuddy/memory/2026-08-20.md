# 2026-08-20

## China Select 项目进度
- 架构文档 `china_select_architecture.md` 锁定支付主体：**Brand partner Co., Ltd（香港）作 Stripe Connect 平台方，各 vendor 作 connected account**；样品走 Stripe（款项直达子账号+平台抽佣），大货走对公+Escrow.com。
- 后台 `china-select-backend/server.js`（零依赖 Node）新增：每家 vendor 的 Stripe Connected Account 配置、订单管理（样品/大货双轨）、样品单 Stripe Checkout 生成（Connect REST 直连，无 stripe npm 包）、Webhook 手验签名置 paid。curl 全流程验证通过。
- 之前一条“无法回答”是误触发（内容合法，香港公司 Stripe Connect 方案），已继续完成。

## 待办（用户未决）
- 接真实 Stripe key（STRIPE_SECRET_KEY / WEBHOOK_SECRET）才能真收款；现在无 key 走 mock。
- AI 改写接入 LLM key（aiRewrite 占位）。
- 真实公司数据填进 Sponsored Partners 区与各家产品（partner1=已映射 demo1；partner2..6 为占位 vendor 用户名，需注册对应账号）。

## 已完成（补充）
- **任务3 完成**：A 站联系表单从 mailto 改为 fetch 提交到后端 `/api/public/inquiry`（免登录，按 partner 的 data-vendor 用户名路由到对应 vendor 账号；未知 vendor 落平台收件箱 `__platform__`，不丢线索）。后端加 CORS（`CORS_ORIGIN`，默认 *）+ OPTIONS 预检。curl 验证通过。A 站伙伴卡加 `data-vendor`，表单新增 partner 下拉（由卡片动态生成）。
- **任务2 部署准备**：CloudStudio 仅支持纯静态站，跑不了 Node 服务 → 改用 Render。已加 `package.json`（start: node server.js，零依赖）+ `render.yaml`（Blueprint 一键）+ README 部署步骤。实际"点 Deploy"需用户自己的 Render 账号（本环境无其凭证，无法代部署）。部署后须把 A 站 `BACKEND` 常量改为 Render URL、并设置 `CORS_ORIGIN` 为 A 站域名。
- **买家 Supply Directory 搜索页完成**：新建 `directory.html`（A 站域名下的独立静态页，导航已加链接）。后端新增两个免登录公开接口：`GET /api/public/products?q=&category=&market=`（跨所有 vendor 搜产品，返回 vendor/hasStripe）和 `POST /api/public/order-sample`（买家免登录下样品单→Stripe Checkout，钱直达 vendor 子账号）。页面支持搜索/分类/市场过滤、Inquire（走公开询盘接口）、Order sample（有 Stripe 子账号→跳转 Checkout；无→转询盘要样）。`BACKEND` 可用 `?backend=` 覆盖便于部署测试。curl 全流程验证通过并已提交。
- **买家账号体系完成（C 模式：免登录下单→自动建 buyer 账号+关联订单）**：后端 users 加 `role`（vendor/buyer）+ `email` + `buyerToken`/`tokenExpiry`。`/api/public/order-sample` 按 `buyerEmail` 调 `findOrCreateBuyer` 自动建/查买家账号并关联订单，返回 `buyerToken`。新增买家页（后端同源渲染，避开跨域 cookie 问题）：`/buyer-login`（邮箱+密码注册/登录，支持 `?token=` 免密登录）、`/buyer-dashboard`（看自己样品/大货订单、样品付款、复购、发起大货 escrow `/api/buyer/bulk-request`）。vendor/buyer 接口按 role 隔离（403 互防）。登录支持邮箱。curl 11 项验证全过（guest 下单→token 登录→订单可见→付款→大货请求→vendor 可见→角色隔离→注册→页面200）。
- **已知缺口**：买家免密令牌目前靠下单响应回显/链接传递（无邮件）。接邮件服务（Resend/SendGrid）后应由后端在 guest 下单后自动发含 token 的登录链接，而非前端暴露令牌。
